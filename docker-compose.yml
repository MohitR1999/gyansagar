services:
  main:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8080:8080
    develop:
      watch:
        - path: ./content
          action: sync+restart
          target: /usr/src/app/content